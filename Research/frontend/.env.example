# Leave empty when the API is served from the same origin or through the Vite dev proxy.
# For a split deployment, set this to the backend origin, for example:
# VITE_API_BASE_URL=https://research-api.freedomlabs.in
VITE_API_BASE_URL=
